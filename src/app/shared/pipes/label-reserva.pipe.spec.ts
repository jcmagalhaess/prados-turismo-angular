import { LabelReservaPipe } from './label-reserva.pipe';

describe('LabelReservaPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelReservaPipe();
    expect(pipe).toBeTruthy();
  });
});
