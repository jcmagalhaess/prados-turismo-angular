import { EmptyLabelPipe } from './empty-label.pipe';

describe('EmptyLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new EmptyLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
