import { FormataParcelasPipe } from './formata-parcelas.pipe';

describe('FormataParcelasPipe', () => {
  it('create an instance', () => {
    const pipe = new FormataParcelasPipe();
    expect(pipe).toBeTruthy();
  });
});
